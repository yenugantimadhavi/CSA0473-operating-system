#include<stdio.h>
#include<unistd.h>
main()
{
int pid,pid1,pid2;
if(pid==-1)
{
print("ERROR IN PROCESS CREATION\N");
exit(1);
}
if(pid!=0)
{
pid1=getpid();
printf("\the parent process id is %n",pid1);
}
else
{
pid2=getpid();
printf("\n the child process ID is %d\n",pid2);
}
}
